# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "inventory_DELETE_1698321384.csv"
    fields:
      - "api_version"
      - "tenant_group_id"
      - "_export_location_graph_status_id"
    skipHeader: true
  overrideAuth:
    type: "noauth"

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/inventory/impex/{{ api_version }}/organizations/{{ tenant_group_id }}/location-graph/exports/{{ _export_location_graph_status_id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 204

  - name: "API request with validation failure"
    flow:
      - delete:
          url: "/inventor/impex/{{ api_version }}/organizations/{{ tenant_group_id }}/location-graph/exports/{{ _export_location_graph_status_id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - delete:
          url: "/inventory/impex/{{ api_version }}/organizations/{{ tenant_group_id }}/location-graph/exports/{{ _export_location_graph_status_id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          expect:
            - statusCode: { not: [200] }
            - hasProperty: "error"
