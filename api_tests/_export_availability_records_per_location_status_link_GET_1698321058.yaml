# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
scenarios:
  # Test scenario for successful API request
  - name: "Successful API request"
    flow:
      - get: 
          url: "/{{_export_availability_records_per_location_status_link}}"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
          expect:
            - statusCode: 200
            - hasProperty: "download"
            - hasProperty: "download.downloadLink"
  
  # Test scenario with validation failure
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/someInvalidUrl"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
          expect:
            - statusCode: 400

  # Test scenario with edge case
  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{_export_availability_records_per_location_status_link}}"
          headers:
            Authorization: "{{ ${very_large_string} }}"
          expect:
            - statusCode: "{{ $processEnvironment.response_range }}"
            - hasProperty: "download"
            - hasProperty: "download.downloadLink"
