# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my_processor.js"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "_export_availability_records_per_location_group_status_link_GET_1698320875.csv"
    fields:
      - "_export_availability_records_per_location_group_status_link"
    skipHeader: true
  ensure:
    p95: 200
  environments:
    staging:
      target: "http://api.staging.test.com"
    production:
      target: "http://api.test.com"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "{{ _export_availability_records_per_location_group_status_link }}"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
          capture:
            json: "$.download.downloadLink"
            as: "download_link"
          expect:
            - statusCode: 200
            - hasProperty: "download"
            - hasProperty: "download.downloadLink"
  - name: "API request with validation failure"
    flow:
      - get:
          url: "{{ invalid_export_availability_records_per_location_group_status_link }}"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
          expect:
            - statusCode: 400
  - name: "API request with edge case"
    flow:
      - get:
          url: "{{ _export_availability_records_per_location_group_status_link }}"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
          capture:
            json: "$.download.downloadLink"
            as: "download_link"
          expect:
            - statusCode: 200
