# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "_export_availability_records_per_location_download_link_GET_1698321080.csv"
    fields: ["_export_availability_records_per_location_download_link"]
  plugins:
    expect: {}

scenarios:
  - name: 'Successful API Request'
    flow:
      - get:
          url: "/{{_export_availability_records_per_location_download_link}}"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
          expect:
            - statusCode: 200
            - hasProperty: "deltaTokenData.deltaToken"

  - name: 'API request with validation failure'
    flow:
      - get:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
          expect:
            - statusCode: 400

  - name: 'API request with edge case'
    flow:
      - get:
          url: "/{{_export_availability_records_per_location_download_link}}?edgeCaseParam=1111111111111111111111111111111111"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
          expect:
            - statusCode: [200, 500]
            - hasProperty: "deltaTokenData.deltaToken"
