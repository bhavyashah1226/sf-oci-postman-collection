# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  createCollection: true
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "inventory_POST_1698321235.csv"
    fields:
      - "api_version"
      - "tenant_group_id"
  scenarios:
    - name: "Successful API request"
      flow:
        - post:
            url: "/inventory/impex/{{api_version}}/organizations/{{tenant_group_id}}/location-graph/exports"
            headers:
              Authorization: "{{ _authorization_key }}"
              Content-Type: "application/json"
            expect:
              - statusCode: 200
              - hasProperty: "exportId"
              - hasProperty: "exportStatusLink"
    - name: "API request with validation failure"
      flow:
        - post:
            url: "/invalid/url"
            headers:
              Authorization: "{{ _authorization_key }}"
              Content-Type: "application/json"
            expect:
              - statusCode: 400
    - name: "API request with edge case"
      flow:
        - post:
            url: "/inventory/impex/{{api_version}}/organizations/{{tenant_group_id}}/location-graph/exports"
            json:
              unknownField: "Edge case data"
            headers:
              Authorization: "{{ _authorization_key }}"
              Content-Type: "application/json"
            expect:
              - statusCode: "{{ _status_code }}"
