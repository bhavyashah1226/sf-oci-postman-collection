# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 1
      arrivalRate: 1

scenarios:
  - name: Successful API request
    flow:
      - post:
          url: "/inventory/impex/{{ $processEnvironment.api_version }}/organizations/{{ $processEnvironment.tenant_group_id }}/availability-records/exports"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          json:
            objects:
              locations: ["{{ location_id }}"]
          expect:
            - statusCode: 200
            - hasProperty: 'exportId'
            - hasProperty: 'exportStatusLink'

  - name: API request with validation failure
    flow:
      - post:
          url: "/dummy"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          json:
            objects:
              locations: ["{{ location_id }}"]
          expect:
            - statusCode: 404

  - name: API request with edge case
    flow:
      - post:
          url: "/inventory/impex/{{ $processEnvironment.api_version }}/organizations/{{ $processEnvironment.tenant_group_id }}/availability-records/exports"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          json:
            objects:
              locations: ["<<<EDGE CASE HERE>>>"]
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'message'
