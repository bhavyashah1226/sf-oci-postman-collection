# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: Successful API request
    flow:
      - get:
          url: "{{ _export_availability_records_per_location_group_download_link }}"
          headers:
            Authorization: "{{ _authorization_key }}"
          expect:
            - statusCode: 200
            - hasProperty: '_export_availability_records_per_location_group_delta_token'

  - name: API request with validation failure
    flow:
      - get:
          url: "invalidUrl"
          headers:
            Authorization: "{{ _authorization_key }}"
          expect:
            - statusCode: 400

  - name: API request with edge case
    flow:
      - get:
          url: "/some/very/long/url/that/is/definitely/not/going/to/work"
          headers:
            Authorization: "{{ _authorization_key }}"
          expect:
            - statusCode: "{{ $processEnvironment.STATUS_CODE }}"
            - hasProperty: "special_key"
