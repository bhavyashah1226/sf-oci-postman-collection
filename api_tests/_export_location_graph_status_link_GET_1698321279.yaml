# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: Successful API request
    flow:
      - get:
          url: "{{ $processEnvironment._export_location_graph_status_link }}"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "download"
            - hasProperty: "download.downloadLink"
  - name: API request with validation failure
    flow:
      - get:
          url: "invalid_url"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 400
  - name: API request with edge case
    flow:
      - get:
          url: "{{ $processEnvironment._export_location_graph_status_link }}"
          headers:
            Authorization: "EdgeCaseToken!"
            Content-Type: "application/json"
          expect:
            - statusCode: 403
            - hasProperty: "errorMessage"
