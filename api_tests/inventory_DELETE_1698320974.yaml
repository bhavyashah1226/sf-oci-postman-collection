# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - delete: "/inventory/impex/{{api_version}}/organizations/{{tenant_group_id}}/availability-records/exports/{{_export_availability_records_per_location_group_id}}"
        secure: true
        headers:
          Authorization: "{{ _authorization_key }}"
          Content-Type: "application/json"
        expect:
          - statusCode: 204

  - name: "API request with validation failure"
    flow:
      - delete: "/invalid_url"
        secure: true
        headers:
          Authorization: "{{ _authorization_key }}"
          Content-Type: "application/json"
        expect:
          - statusCode: 400

  - name: "API request with edge case"
    flow:
      - delete: "/inventory/impex/{{api_version}}/organizations/{{tenant_group_id}}/availability-records/exports/{{_export_availability_records_per_location_group_id}}"
        secure: true
        headers:
          Authorization: "{{ _authorization_key }}"
          Content-Type: "application/json"
        expect:
          - statusCode: { "in": [200, 204, 304] }
