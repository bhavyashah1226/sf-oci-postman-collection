# Test generated by RoostGPT for test demo1 using AI Type Azure Open AI and AI Model roost-gpt4-32k

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "_export_location_graph_download_link_GET_1698321311.csv"
    fields:
      - "_export_location_graph_download_link"
    skipHeader: true
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "{{ _export_location_graph_download_link }}"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "groups"
            - hasProperty: "locations"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "invalid_url"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 400
          
  - name: "API request with edge case"
    flow:
      - get:
          url: "{{ _export_location_graph_download_link }}"
          headers:
            Authorization: "{{ $processEnvironment._authorization_key }}"
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $processEnvironment.status_code }}"
            - hasProperty: "groups"
            - hasProperty: "locations"
